#include <iostream>
using namespace std;

void foo(int x, int* y, int& z);

int main() 
{
  int a = 5;
  cout << a << " " << &a << endl;
  foo(a, &a, a);
}
void foo(int x, int* y, int& z){
  cout << x << " " << &x << endl;
  cout << y << " " << &y << " " << *y << endl;
  cout << z << " " << &z << endl;
}
/*
Output:

5 0x7ffce1fd4b84
5 0x7ffce1fd4b6c
0x7ffce1fd4b84 0x7ffce1fd4b60 5
5 0x7ffce1fd4b84
שורה ראשונה: cout << a << " " << &a << endl;

5: ערך של a.
0x7ffce1fd4b84: הכתובת בזיכרון שבה a מאוחסן.
שורה שנייה: cout << x << " " << &x << endl; בתוך foo()

5: ערך של x בתוך foo(). זהו העתק של הערך של a שנשלח לפונקציה.
0x7ffce1fd4b6c: הכתובת בזיכרון שבה x מאוחסן. זה שונה מהכתובת של a כי x הוא משתנה חדש בתוך הפונקציה.
שורה שלישית: cout << y << " " << &y << " " << *y << endl; בתוך foo()

0x7ffce1fd4b84: הכתובת שבה y (הפוינטר) מצביע, שהיא הכתובת של a.
0x7ffce1fd4b60: הכתובת של הפוינטר עצמו (הכתובת של משתנה y).
5: הערך שy מצביע עליו, כלומר הערך של a.
שורה רביעית: cout << z << " " << &z << endl; בתוך foo()

5: ערך של z. זהו ערך של a משום ש-z הוא רפרנס ל-a.
0x7ffce1fd4b84: הכתובת בזיכרון שבה z מאוחסן, שהיא בדיוק הכתובת של a.
סיכום:
x הוא משתנה מקומי בתוך הפונקציה foo, ולכן יש לו כתובת שונה מהכתובת של a.
y הוא פוינטר לכתובת של a, ולכן y עצמו מצביע לכתובת של a, וערך שy מצביע עליו הוא 5. הכתובת של הפוינטר y היא שונה מהכתובת של a.
z הוא רפרנס ל-a, ולכן הכתובת של z היא בדיוק הכתובת של a, והערך שלה הוא 5.
*/
